# Локализация приложения

## Реализованные возможности

### 1. Система локализации
- ✅ Создан контекст `LanguageContext` для управления языком приложения
- ✅ Поддержка русского (🇷🇺) и английского (🇬🇧) языков
- ✅ Автоматическое сохранение выбранного языка в localStorage
- ✅ Динамическая загрузка переводов

### 2. Переключатель языка
- ✅ Расположен в TitleBar рядом с логотипом SHAKEDOWN
- ✅ Отображает флаг и название текущего языка
- ✅ Выпадающее меню с анимацией для выбора языка
- ✅ Индикатор активного языка (галочка)
- ✅ Закрытие меню при клике вне его области

### 3. Локализованные компоненты
- ✅ **TitleBar** - кнопки управления окном
- ✅ **Sidebar** - навигация и кнопка выхода
- ✅ **HomePage** - серверы, статусы загрузки, сообщения об ошибках
- ✅ **SettingsPage** - все настройки и их описания

## Структура файлов

```
src/
├── contexts/
│   └── LanguageContext.tsx    # Контекст локализации
├── locales/
│   ├── ru.ts                  # Русские переводы
│   └── en.ts                  # Английские переводы
├── components/
│   ├── TitleBar.tsx           # С переключателем языка
│   └── Sidebar.tsx            # Локализованная навигация
└── pages/
    ├── HomePage.tsx           # Локализованная главная страница
    └── SettingsPage.tsx       # Локализованные настройки
```

## Использование

### Добавление нового перевода

1. Добавьте ключ в `src/locales/ru.ts`:
```typescript
export default {
  // ...
  'new.key': 'Новый текст',
}
```

2. Добавьте тот же ключ в `src/locales/en.ts`:
```typescript
export default {
  // ...
  'new.key': 'New text',
}
```

3. Используйте в компоненте:
```typescript
import { useLanguage } from '../contexts/LanguageContext'

function MyComponent() {
  const { t } = useLanguage()
  
  return <div>{t('new.key')}</div>
}
```

### Программное изменение языка

```typescript
import { useLanguage } from '../contexts/LanguageContext'

function MyComponent() {
  const { language, setLanguage } = useLanguage()
  
  const switchToEnglish = () => {
    setLanguage('en')
  }
  
  return <button onClick={switchToEnglish}>Switch to English</button>
}
```

## Дизайн переключателя

Переключатель языка имеет:
- Современный дизайн с плавными анимациями
- Hover-эффекты для улучшения UX
- Выпадающее меню с анимацией появления
- Галочка для активного языка
- Автоматическое закрытие при клике вне области

## Поддерживаемые языки

| Код | Язык | Флаг |
|-----|------|------|
| `ru` | Русский | 🇷🇺 |
| `en` | English | 🇬🇧 |

## Примечания

- Язык по умолчанию: Русский (`ru`)
- Выбранный язык сохраняется в `localStorage` с ключом `language`
- Все переводы загружаются динамически при смене языка
- Если перевод не найден, отображается ключ перевода
