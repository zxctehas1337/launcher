@echo off
echo 🔐 Локальный вход в админку
echo ═════════════════════════════
echo.

set /p password="Введите пароль: "

if "%password%"=="5732" (
    echo.
    echo ✅ Пароль принят!
    echo 📡 Отправка запроса на сервер...
    echo.

    curl -X POST "http://localhost:8080/api/auth?action=auth_test_admin" ^
         -H "Content-Type: application/json" ^
         -d "{\"password\": \"%password%\"}" ^
         -w "\n\n📊 Статус: %%{http_code}\n" ^
         --silent --show-error

    echo.
    echo.
    echo 💡 Если ответ успешный, используйте данные для входа в админ-панель
) else (
    echo ❌ Неверный пароль!
    echo 💡 Подсказка: пароль состоит из 4 цифр
)

pause
